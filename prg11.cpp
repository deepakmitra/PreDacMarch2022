//if we enter characters more than the specified size of the array
#include<stdio.h>
int main()
{
	char str[10];
	
	printf("\n Enter a string :");
	scanf("%s", str);
	
	printf("\n Entered String : %s", str);
}
